# AuthDemo

A full-scale **authentication system** with a **Vue.js frontend** and a **Node.js + MongoDB backend**.  
The project follows a real-world client–server architecture and is deployed online.

## 🚀 Features
- User registration and login
- RESTful authentication APIs
- Frontend–backend separation
- Persistent user storage with MongoDB
- Cloud deployment

## 🛠 Tech Stack
### Frontend
- Vue.js
- HTML / CSS / JavaScript
- Hosted on Render

### Backend
- Node.js
- Express.js
- MongoDB
- REST APIs

## ▶️ How to Run Locally
### Backend
1. Navigate to backend directory
2. Install dependencies: `npm install`
3. Start server: `npm start`

### Frontend
1. Navigate to frontend directory
2. Install dependencies: `npm install`
3. Run dev server: `npm run serve`
